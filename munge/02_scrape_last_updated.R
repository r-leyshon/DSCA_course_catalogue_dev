#purpose of script: extract last updated

"Issue with this script - the mr3 nodes for last updated were taken from the search results. Now see if possible to take from 
GET response instead"


#extract all course repo names from all_pages url vector
#mr3_cleansed <- lapply(all_pages, extract_mr3) %>% unlist()



#output_dataframe$last_updated <- mr3_cleansed


# remove(list = c(
#                 'mr3_cleansed',
#                 'extract_mr3',
#                 'all_pages'))
